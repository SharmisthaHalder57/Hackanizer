# Core
fastapi
uvicorn[standard]
python-jose[cryptography]
pydantic[email]
python-dotenv
httpx

# Flask gateway
flask
flask-cors
requests

# Firebase
firebase-admin

# QR Code generation  ← NEW (for meal coupon QR codes)
qrcode[pil]
Pillow